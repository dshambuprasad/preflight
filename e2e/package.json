{
  "name": "@preflight/e2e",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Playwright — the demo script (01 §5) as a test against docker compose (11 §4). Spec lands in M1.",
  "scripts": { "test": "playwright test" },
  "devDependencies": { "@playwright/test": "1.63.0", "@types/node": "22.20.2" }
}
